# DockerLampLaravelJenkinsSandwich
 A Docker container that has a LAMP server + a simple Laravel website + continuous integration using Jenkins + docker persistent volumes so MySQL databases are saved locally!
